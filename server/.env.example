# Server Configuration
PORT=5001

# Database
MONGO_URI=mongodb://localhost:27017/law-firm-demo

# Authentication (generate a secure secret for production)
JWT_SECRET=your-secret-key-here

# ===========================================
# AI Configuration
# ===========================================

# Toggle between local (Ollama) and cloud (OpenAI) LLM
# Set to "true" for local Ollama, "false" for cloud OpenAI
USE_LOCAL_LLM=true

# --- Local LLM (Ollama) Configuration ---
# Only used when USE_LOCAL_LLM=true
OLLAMA_URL=http://localhost:11434/api/chat
OLLAMA_MODEL=llama3

# --- Cloud LLM (OpenAI) Configuration ---
# Only used when USE_LOCAL_LLM=false
# Get your API key at: https://platform.openai.com/api-keys
# Pricing: ~$0.15/million input tokens, ~$0.60/million output tokens
OPENAI_API_KEY=your-openai-api-key-here
OPENAI_MODEL=gpt-4o-mini
